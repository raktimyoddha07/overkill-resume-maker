# PostgreSQL connection string for Prisma (required at runtime for API routes)
# Copy to .env.local and adjust credentials for your local database.
DATABASE_URL=

# Directory where uploaded assets will be stored.
# Can be absolute (e.g. C:\my-assets) or relative (e.g. uploads). Defaults to "uploads" in project root if left empty.
UPLOADS_DIR=uploads
